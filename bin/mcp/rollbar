#!/usr/bin/env bash

set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_common.sh"
load_mcp_env

require_env "ROLLBAR_ACCESS_TOKEN" \
  "Set ROLLBAR_ACCESS_TOKEN before using the Rollbar MCP. $(mcp_env_hint)"

exec npx -y @rollbar/mcp-server@latest
